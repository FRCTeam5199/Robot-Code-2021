package frc.motors;

/**
 * This should be one-for-one replicated for each {@link AbstractMotorController motor controller} in order
 * to create settings to switch between motor implementations
 */
public enum SupportedMotors {
    CAN_SPARK_MAX, TALON_FX, VICTOR
}
